import { CycleDirective } from './cycle.directive';

describe('CycleDirective', () => {
  it('should create an instance', () => {
    const directive = new CycleDirective();
    expect(directive).toBeTruthy();
  });
});
